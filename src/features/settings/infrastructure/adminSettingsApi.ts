// Input: api/admin
// Output: 管理类设置 API 封装
// Pos: src/features/settings/infrastructure/adminSettingsApi.ts
// 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

import { adminApi, Role, User } from '../../../api/admin';

export { adminApi };
export type { Role, User };
