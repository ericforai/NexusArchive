// Input: api/audit
// Output: 审计日志 API 封装
// Pos: src/features/settings/api/auditSettingsApi.ts
// 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

import { auditApi, AuditLog } from '../../../api/audit';

export { auditApi };
export type { AuditLog };
