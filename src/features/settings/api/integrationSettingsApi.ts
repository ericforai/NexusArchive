// Input: api/erp
// Output: 集成设置 API 封装
// Pos: src/features/settings/api/integrationSettingsApi.ts
// 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

import { erpApi, ErpConfig, ErpScenario, ErpSubInterface, SyncHistory } from '../../../api/erp';

export { erpApi };
export type { ErpConfig, ErpScenario, ErpSubInterface, SyncHistory };
