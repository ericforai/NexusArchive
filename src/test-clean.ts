// 清洁测试文件 - 无复杂度违规
export const cleanFunction = (n: number): number => {
    return n * 2;
};

export const add = (a: number, b: number): number => {
    return a + b;
};

// 短函数，低复杂度
export const greet = (name: string): string => {
    return `Hello, ${name}!`;
};
