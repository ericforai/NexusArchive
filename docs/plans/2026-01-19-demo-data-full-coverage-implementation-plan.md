# Demo Data Full Coverage Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 生成 2025 年 7 全宗最小规模的全模块演示数据（含附件），并双通道输出到 Flyway 与 `db/seed-data.sql`。

**Architecture:** 使用 Python 生成器读取规格数据，构造链路数据与附件文件，输出 SQL（迁移脚本 + seed-data 注入块），同时生成可预览的 PDF 附件。数据全部使用固定前缀 ID，确保一致性与可回滚。

**Tech Stack:** Python 3（unittest + hashlib），SQL（INSERT 模式），Markdown 文档

---

## Baseline

- Worktree: `/Users/user/nexusarchive/.worktrees/demo-data-2025`
- Baseline tests currently failing in repo (pre-existing): `npm run test:run` fails in multiple suites. Continue with targeted Python tests only.

---

### Task 1: Spec Loader + Generator Skeleton

**Files:**
- Create: `scripts/demo_data_spec.json`
- Create: `scripts/generate_demo_data.py`
- Create: `scripts/test_generate_demo_data.py`

**Step 1: Write the failing test**

```python
import unittest
from pathlib import Path

from generate_demo_data import load_spec

class DemoDataSpecTest(unittest.TestCase):
    def test_load_spec_has_expected_fonds(self):
        spec = load_spec(Path('scripts/demo_data_spec.json'))
        fonds_codes = [f['fonds_code'] for f in spec['fonds']]
        self.assertEqual(
            fonds_codes,
            ['BR-GROUP', 'BR-SALES', 'BR-TRADE', 'BR-MFG', 'COMP001', 'BRJT', 'DEMO'],
        )
```

**Step 2: Run test to verify it fails**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: FAIL with `ModuleNotFoundError` or missing spec file.

**Step 3: Write minimal implementation**

```python
# scripts/generate_demo_data.py
# Input: spec json
# Output: parsed spec dict
# Pos: demo data generator
# 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

import json
from pathlib import Path

def load_spec(spec_path: Path) -> dict:
    return json.loads(spec_path.read_text(encoding='utf-8'))
```

Add `scripts/demo_data_spec.json` with the 7 fonds + chain metadata.

**Step 4: Run test to verify it passes**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/demo_data_spec.json scripts/generate_demo_data.py scripts/test_generate_demo_data.py
git commit -m "feat: add demo data spec loader"
```

---

### Task 2: Archive Chain Generation + Relation Validation

**Files:**
- Modify: `scripts/generate_demo_data.py`
- Modify: `scripts/test_generate_demo_data.py`

**Step 1: Write the failing test**

```python
from generate_demo_data import build_archives, build_relations, load_spec

class DemoArchiveTest(unittest.TestCase):
    def test_archive_counts_and_relations(self):
        spec = load_spec(Path('scripts/demo_data_spec.json'))
        archives = build_archives(spec)
        relations = build_relations(spec, archives)
        self.assertEqual(len(archives), 7 * 6)
        archive_ids = {a['id'] for a in archives}
        for rel in relations:
            self.assertIn(rel['source_id'], archive_ids)
            self.assertIn(rel['target_id'], archive_ids)
```

**Step 2: Run test to verify it fails**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: FAIL with `NameError` for missing functions.

**Step 3: Write minimal implementation**

```python
def build_archives(spec: dict) -> list[dict]:
    # return list of acc_archive rows (dicts)
    return []

def build_relations(spec: dict, archives: list[dict]) -> list[dict]:
    return []
```

Then implement actual chain generation:
- 每全宗生成 6 个节点（合同/发票/凭证/付款单/回单/报表）
- 使用 `demo-2025-<fonds>-<type>-001` ID 前缀
- 金额与日期在链路内保持一致

**Step 4: Run test to verify it passes**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/generate_demo_data.py scripts/test_generate_demo_data.py
git commit -m "feat: generate demo archive chains"
```

---

### Task 3: Pre-Archive + Attachment Generation

**Files:**
- Modify: `scripts/generate_demo_data.py`
- Modify: `scripts/test_generate_demo_data.py`
- Create: `docs/demo数据/*.pdf` (generated by script)

**Step 1: Write the failing test**

```python
from generate_demo_data import build_file_contents, write_demo_files

class DemoAttachmentTest(unittest.TestCase):
    def test_files_and_hashes(self):
        spec = load_spec(Path('scripts/demo_data_spec.json'))
        archives = build_archives(spec)
        files = build_file_contents(spec, archives)
        self.assertTrue(all(f['file_size'] > 0 for f in files))
```

**Step 2: Run test to verify it fails**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: FAIL with missing functions.

**Step 3: Write minimal implementation**

Implement:
- `build_file_contents` 生成 `arc_file_content` + `acc_archive_attachment` 的数据
- `write_demo_files(output_dir)` 生成最小 PDF（自带 PDF 模板，无外部依赖）
- `file_hash` 使用 `sha256`，`hash_algorithm` 设为 `SHA256`

**Step 4: Run test to verify it passes**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/generate_demo_data.py scripts/test_generate_demo_data.py docs/demo数据
git commit -m "feat: generate demo attachments and hashes"
```

---

### Task 4: SQL Rendering + Dual Output + Docs Update

**Files:**
- Modify: `scripts/generate_demo_data.py`
- Modify: `scripts/test_generate_demo_data.py`
- Create: `nexusarchive-java/src/main/resources/db/migration/V20260119__seed_demo_full_data.sql`
- Modify: `db/seed-data.sql`
- Modify: `scripts/README.md`
- Create: `docs/demo数据/README.md`
- Modify: `docs/plans/README.md`
- Modify: `docs/CHANGELOG.md`

**Step 1: Write the failing test**

```python
from generate_demo_data import render_sql_blocks

class DemoSqlRenderTest(unittest.TestCase):
    def test_sql_contains_markers(self):
        sql = render_sql_blocks(load_spec(Path('scripts/demo_data_spec.json')))
        self.assertIn('-- DEMO DATA START', sql)
        self.assertIn('INSERT INTO acc_archive', sql)
```

**Step 2: Run test to verify it fails**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: FAIL with missing renderer.

**Step 3: Write minimal implementation**

Implement:
- `render_sql_blocks(spec)` to emit deterministic `INSERT` SQL for all required tables
- `write_outputs()` to:
  - 写入 `V20260119__seed_demo_full_data.sql`
  - 在 `db/seed-data.sql` 的 `-- PostgreSQL database dump complete` 前注入标记块
  - 生成 `docs/demo数据/*.pdf`
- 更新文档清单与变更记录

**Step 4: Run test to verify it passes**

Run: `python3 -m unittest scripts/test_generate_demo_data.py -v`  
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/generate_demo_data.py scripts/test_generate_demo_data.py \
  nexusarchive-java/src/main/resources/db/migration/V20260119__seed_demo_full_data.sql \
  db/seed-data.sql docs/demo数据/README.md scripts/README.md docs/plans/README.md docs/CHANGELOG.md
git commit -m "feat: seed full demo data for 2025"
```

---

## Execution Notes

- 执行 `python3 scripts/generate_demo_data.py --write` 生成 SQL 与附件。
- 如需手动验证：运行 SQL 校验脚本（JOIN 验证关系、附件路径存在、案卷数量一致）。
- 仅使用 Python 测试，不跑前端 `npm run test:run`（基线已失败）。
