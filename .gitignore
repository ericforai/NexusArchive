# Input: 版本控制/构建忽略规则
# Output: 忽略规则配置
# Pos: 仓库配置
# 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

# ============================================
# NexusArchive .gitignore
# 私有仓库优化版 - 最大化两台 Mac 之间的同步
# ============================================

# ----------------
# 必须忽略（运行时生成/太大/每台电脑不同）
# ----------------

# Node.js 依赖（太大，用 npm install 安装）
node_modules

# Java 编译产物（用 mvn 构建重新生成）
nexusarchive-java/target/

# 前端构建产物（用 npm run build 重新生成）
dist
dist-ssr
build
.cache

# 运行时 PID 文件（每次启动重新生成）
.run/

# 日志文件（运行时生成）
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# macOS 系统文件（每台电脑自动生成，内容不同）
.DS_Store

# 临时编辑器交换文件
*.sw?
*.suo

# ----------------
# 可选忽略（根据需要取消注释）
# ----------------

# IDE 配置（如果两台电脑用不同 IDE 配置则取消注释）
# .idea
# .vscode/*
# !.vscode/extensions.json

# ----------------
# 项目特定：构建发布包
# ----------------
*.tar.gz
nexusarchive-release.*

# ----------------
# 项目特定：备份文件和数据库备份（严禁同步）
# ----------------
# 备份目录和文件
backup*/
backup_*
backups/
*.backup
*.bak
# macOS 资源分叉文件
._backup*

# 数据库备份文件（但保留脚本目录中的 SQL 脚本）
*.dump
backup*.sql
*_backup.sql
*_prod_*.sql
*_backup_*.sql
# 根目录下的 SQL 文件（通常是备份）
/*.sql
# 但允许脚本目录中的 SQL 文件
!scripts/*.sql
!deploy/**/*.sql
!nexusarchive-java/src/main/resources/**/*.sql
!data/init_*.sql

# ----------------
# 项目特定：测试/临时文件
# ----------------
test_aip*/
.m2-temp/
test_upload.pdf

# ----------------
# 项目特定：数据文件（如果太大或包含测试数据）
# ----------------
# 档案存储目录（严禁同步业务数据）
nexusarchive-java/data/
uploads/
nexusarchive-java/uploads/

# 临时审批数据（如果需要同步则注释掉）
# pending_list.json
# temp_approvals.json

# 密钥与证书文件（严禁同步到公共仓库）
nexusarchive-java/keystore/
*.pem
*.p12
*.jks

# 敏感/大体积 Demo 数据
docs/demo数据/

# ----------------
# 环境变量配置文件（严禁同步真实配置）
# ----------------
# 忽略所有 .env 文件（包含敏感信息）
.env
.env.local
.env.prod
.env.prod.bak
.env.host.dev
# 但允许模板文件同步
!.env.example
!.env.template
!.env.prod.example
!.env.prod.template

# ----------------
# 需要同步的重要文件（确保没有被忽略）
# ----------------
# .env.example         ← 会同步（模板）
# .env.template        ← 会同步（模板）
# .env.prod.example    ← 会同步（生产环境模板）
# .idea/               ← 已移除忽略，会同步 IDE 配置
# .vscode/             ← 已移除忽略，会同步 IDE 配置
# scripts/             ← 会同步所有脚本
# docs/                ← 会同步所有文档

# Large Binaries
deploy/offline/bin/*.jar
