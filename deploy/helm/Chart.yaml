# Input: Helm 模板变量
# Output: Kubernetes 资源配置
# Pos: K8s 部署配置
# 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

apiVersion: v2
name: nexusarchive
description: Helm chart for NexusArchive (backend + frontend)
type: application
version: 0.1.0
appVersion: "2.0.0"
